export enum AppRoutes {
    METHODOLOGY = 'methodology',
    PROJECT = 'project',
    SETTINGS = 'settings',
    TEST = 'test'
}

export enum TestRoutes {
    OVERIEW = 'overview',
    SECTION = 'section',
    TASK = 'task'
}

export enum AppParams {
    ID = ':id'
}
